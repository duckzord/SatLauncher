
package Window;

import javax.swing.*;
import Main.Main;

/*public class WindowSetup {
    
    public static void WindowSetup() {
        
        GLCapabilities caps = new GLCapabilities();
        caps.setDoubleBuffered(true);// request double buffer display mode
        GLJPanel canvas = new GLJPanel(caps);
        FPSAnimator animator = new FPSAnimator(canvas, 60); // Constructs animator with initial thing to draw (canvas) and the frames per second
        Main program = new Main();
        canvas.addGLEventListener(program);
        canvas.addKeyListener(program);
        JFrame.setDefaultLookAndFeelDecorated(false);
        JFrame frame = new JFrame("Equation Tester");
        //JLabel boxlabel = new JLabel("x0: ", JLabel.RIGHT);
        //JTextField initialx = new JTextField(3);
        //canvas.add(initialx);
        frame.setSize(500, 500);
        frame.setLocationRelativeTo(null);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
        frame.getContentPane().add(canvas);
        frame.setVisible(true);
        canvas.requestFocusInWindow();
        animator.start();
    }
    
}*/
